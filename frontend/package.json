{
  "name": "@hackathon-kiro/frontend",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "eslint src --ext .ts",
    "format": "prettier --write src"
  },
  "dependencies": {
    "phaser": "3.80.1"
  },
  "devDependencies": {
    "fast-check": "3.22.0",
    "typescript": "5.7.3",
    "vite": "5.4.0",
    "vitest": "1.6.0"
  }
}
